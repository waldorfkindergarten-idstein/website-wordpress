.venv/
latest.zip
.env
*:Zone.Identifier

# Extracted design source, ~29 MB. A build input, not code — ignored so a bulk
# `git add` cannot drop it into history, where it would stay for good.
design.html

wordpress/wp-config.php
wordpress/.htaccess

wordpress/wp-content/cache/
wordpress/wp-content/upgrade/
wordpress/wp-content/uploads/
wordpress/wp-content/languages/

wordpress/oc-bootstrap-setup.php
wordpress/oc-fix-formulare.php

node_modules/

# Visual-Regression: Screenshots bleiben lokal (~55 MB), der Geometrie-
# Fingerabdruck baseline/geometry.json ist Text und wird eingecheckt.
tests/visual/*/*.png
tests/visual/diff-*/
tests/visual/verify/

# One-off database dump for the staging import. Contains user password
# hashes and e-mail addresses — never commit it.
waldorf-staging.sql

# Source documents from the Verein (Leitbild, webKITA sheet, Terminkalender).
# Working drafts, not site content — the facts they carry live in the theme,
# the Termine post type and the legal pages. Kept locally, out of the repo.
*.docx
*.docx:Zone.Identifier
Terminkalender*.pdf
